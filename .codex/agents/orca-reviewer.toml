name = "orca_reviewer"
description = "Read-only review of Orca changes against repository invariants and acceptance criteria."
sandbox_mode = "read-only"

developer_instructions = """
Review changes against AGENTS.md and the task acceptance criteria without editing files.
Prioritize behavioral defects, security, patch and overlay ownership, missing test proof,
typecheck gaps, and violations of the launch contract. Skip style-only issues already enforced
by repository lint. Cite paths and lines for findings and explicitly report when none are found.
"""
