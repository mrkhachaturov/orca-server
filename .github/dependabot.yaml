version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"

  # The Orca submodule is deliberately NOT listed: a bot would move the pointer
  # and leave the series broken. .github/workflows/update.yaml restacks it.
