.cache
.DS_Store
node_modules

# AppImage build export dir (buildx --output type=local)
/dist

# Quilt's internal data.
/.pc
/patches/*.diff~

# Local-only agent tooling: machine-specific paths, never the repo's contract.
# The contract itself is AGENTS.md + CLAUDE.md, and .claude/skills/ ships with it;
# only what depends on a local index or a local machine stays out.
/mise.local.toml
/.mcp.json
/CLAUDE.local.md
/.claude/settings.local.json
/.claude/findings.md
/.claude/skills/orca-graph/
/.claude/skills/orca-wiki/
