.cache
.DS_Store
node_modules

# AppImage build export dir (buildx --output type=local)
/dist

# Quilt's internal data.
/.pc
/patches/*.diff~

# Local-only agent tooling: machine-specific paths, never the repo's contract.
/mise.local.toml
/.mcp.json
/.claude
